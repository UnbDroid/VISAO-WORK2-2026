import cv2
import sys


imagem=cv2.imread("fotofita.jpg(1)")
cv2.imshow("Imagem", imagem)
cv2.waitKey(0)
cv2.destroyAllWindows()
#abrindo o video da camera que sera conectada a raspberry
#video = cv2.VideoCapture(0)
nome_tracker = "CSRT"
tracker = cv2.TrackerCSRT_create()

print(f"Camera abriu?{video.IsOpened()}")

if not video.IsOpened():
    print('Não foi possível carregar o vídeo')
    sys.exit()

ok, frame = video.read() 
if not ok: 
    print("Não foi possivel ler o arquivo de vídeo")
    sys.exit() 

#Criando agora a classe para detecção das fitas 
class FitaDetector:
    def __init__(self):
        